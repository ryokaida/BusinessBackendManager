# # This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# # For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

# # Sources for the syntax:
# # https://docs.github.com/en/actions/writing-workflows/quickstart
# # https://docs.github.com/en/actions/use-cases-and-examples/building-and-testing/building-and-testing-nodejs

# name: Node.js CI

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# # Sources:
# # https://github.com/mongodb/atlas-github-action
# # https://www.mongodb.com/docs/atlas/configure-api-access/
# env:
#   MONGODB_ATLAS_PUBLIC_API_KEY: ${{ secrets.MONGODB_PUBLIC_API_KEY }}
#   MONGODB_ATLAS_PRIVATE_API_KEY: ${{ secrets.MONGODB_PRIVATE_API_KEY }}
#   MONGODB_ATLAS_PROJECT_ID: ${{ secrets.MONGODB_PROJECT_ID }}

# jobs:
#   buildAndTest:

#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [23.x]
#         # See supported Node.js release schedule at https://nodejs.org/en/about/releases/
#         python-version: [3.13.2]

#     steps:
#     - uses: actions/checkout@v4
#     # load secrets as environment variables
#     # source: https://www.baeldung.com/ops/github-actions-env-file
#     - name: Create .env File
#       run: |
#         touch .env
#         echo "${{ secrets.PROJECT1_SECRETS }}" >> .env
#     # get current IP
#     # source: https://stackoverflow.com/questions/75839566/why-cant-i-connect-to-mongodb-atlas-with-github-actions
#     - name: Get current ip
#       id: get_gh_runner_ip
#       shell: bash
#       run: |
#         echo "ip_address=$(curl https://checkip.amazonaws.com)" >> "$GITHUB_OUTPUT"
#     # install MongoDB Atlas CLI to use Atlas CLI GitHub Action by mongodb from Marketplace
#     - name: setup MongoDB Atlas CLI
#       uses: mongodb/atlas-github-action@v0.2.0
#     # temporarily add current IP to allow list in MongoDB Atlas
#     # source: https://stackoverflow.com/questions/75839566/why-cant-i-connect-to-mongodb-atlas-with-github-actions
#     - name: Add agent IP to MongoDB access list
#       shell: bash
#       run: |
#         atlas accessList create ${{ steps.get_gh_runner_ip.outputs.ip_address }} --type ipAddress --projectId ${{ env.MONGODB_ATLAS_PROJECT_ID }}
#     # set up pytest
#     - name: set up Python ${{ matrix.python-version }}
#       uses: actions/setup-python@v4
#       with:
#         python-version: ${{ matrix.python-version }}
#     # Sources:
#     # https://pytest-with-eric.com/integrations/pytest-github-actions/
#     # https://stackoverflow.com/questions/77353375/why-i-get-no-module-named-requests-within-the-github-actions-runner
#     # https://stackoverflow.com/questions/65090928/github-pytest-action-failing-to-import-from-tests-folder
#     # https://github.com/GroenteLepel/qiskit-quantum-knn/blob/master/pytest.ini
#     - name: Install Python dependecies
#       run: |
#         python -m pip install --upgrade pip
#         pip install pytest
#         pip install requests
#     # set up node
#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v4
#       with:
#         node-version: ${{ matrix.node-version }}
#         cache: 'npm'
#     # run npm scripts
#     - run: npm ci
#     - run: npm run build --if-present
#     - run: npm test
#     # remove current agent's IP from allow list in MongoDB Atlas
#     # source: https://stackoverflow.com/questions/75839566/why-cant-i-connect-to-mongodb-atlas-with-github-actions
#     - name: Remove agent IP from MongoDB Access List
#       shell: bash
#       run: |
#         atlas accessLists delete ${{ steps.get_gh_runner_ip.outputs.ip_address }} --projectId ${{ env.MONGODB_ATLAS_PROJECT_ID }} --force
   
#   deploy:

#     runs-on: ubuntu-latest

#     needs: buildAndTest # ensure that test was successful before testing

#     steps:
#       - uses: actions/checkout@v4
#       # source: https://github.com/marketplace/actions/render-deploy-action
#       - name: DeployToRender
#         uses: johnbeynon/render-deploy-action@v0.0.8
#         with:
#           service-id: ${{ secrets.MY_RENDER_SERVICE_ID }}
#           api-key: ${{ secrets.MY_RENDER_API_KEY }}
#           wait-for-success: true
